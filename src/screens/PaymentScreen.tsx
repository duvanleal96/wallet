import React from 'react';
import { Text, View } from 'react-native';
import { MyStackScreenProps } from '../interface/MyStackScreenProps';
import { MovementsList } from '../components/molecules/MovementList';

export const PaymentScreen = ({ navigation }: MyStackScreenProps) => {
  return (
    <View>
      <Text>Hola</Text>
    </View>
  );
};
