import React from 'react';
import { Text, View } from 'react-native';
import { MyStackScreenProps } from '../interface/MyStackScreenProps';

export const LoansScreen = ({ navigation }: MyStackScreenProps) => {
  return (
    <View>
      <Text>Saludos aqui tmbien</Text>
    </View>
  );
};
