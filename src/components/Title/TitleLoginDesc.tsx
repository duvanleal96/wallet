import { Text } from 'react-native';
import React from 'react';
import { stylesLoginUser } from '../../theme/LoginUserTheme';

const TitleLoginDesc = () => {
  return <Text style={stylesLoginUser.title}>Login or sing up for free</Text>;
};

export default TitleLoginDesc;
