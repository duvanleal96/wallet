import { Text } from 'react-native';
import React from 'react';
import { stylesLoginUser } from '../../theme/LoginUserTheme';

const TitleLogin = () => {
  return <Text style={stylesLoginUser.text}>MATERIAL 2 UI</Text>;
};

export default TitleLogin;
